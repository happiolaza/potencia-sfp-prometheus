{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "potencia-sfp-barracas.serviceAccountName" . }}
  labels:
{{ include "potencia-sfp-barracas.labels" . | nindent 4 }}
{{- end -}}
